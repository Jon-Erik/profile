{
  "name": "profile",
  "version": "1.0.0",
  "description": "Personal website",
  "homepage": "http://jon-erik.github.io/profile",
  "scripts": {
    "start": "rm -rf dist && parcel ./src/index.html",
    "build": "rm -rf dist && parcel build ./src/index.html --dist-dir dist --public-url https://jon-erik.github.io/profile",
    "test": "echo \"Error: no test specified\" && exit 1",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.2.0",
    "modern-normalize": "^3.0.1",
    "parcel-bundler": "^1.12.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0"
  },
  "devDependencies": {
    "@parcel/transformer-stylus": "^2.12.0",
    "parcel": "^2.12.0"
  }
}
